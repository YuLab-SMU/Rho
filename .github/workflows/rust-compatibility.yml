name: Rust Compatibility

on:
  push:
    branches: [main]
    paths:
      - "**/Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".cargo/**"
      - "crates/**/*.rs"
      - "desktop/src-tauri/**"
      - "R/rho.agent/R/aisdk_adapter.R"
      - "desktop/dist/app.js"
      - "desktop/dist/index.html"
      - "desktop/dist/styles.css"
      - "LICENSE"
      - "LICENSES.md"
      - "PRIVACY.md"
      - "SECURITY.md"
      - "CODE_SIGNING_POLICY.md"
      - "README.md"
      - "NEWS.md"
      - ".github/CODEOWNERS"
      - ".github/release-notes/**"
      - "vendor/jet/**/*.rs"
      - "runtime/ark.json"
      - "scripts/bootstrap-ark-macos.sh"
      - "scripts/bootstrap-ark-windows.ps1"
      - "scripts/build-windows-installer.ps1"
      - "scripts/tauri-bundle-type.mjs"
      - ".github/workflows/rust-compatibility.yml"
      - ".github/workflows/rust-fast.yml"
      - ".github/workflows/candidate-build-draft.yml"
      - ".github/workflows/candidate-publish.yml"
      - ".github/workflows/native-updater-acceptance-target.yml"
      - ".github/workflows/native-updater-acceptance-window.yml"
      - ".github/workflows/signpath-free-trial-smoke.yml"
      - ".github/workflows/windows-issue33-installed-acceptance.yml"
      - ".github/workflows/update-site-publish.yml"
      - "scripts/candidate-release.mjs"
      - "scripts/generate-update-site.mjs"
      - "scripts/macos-notary.mjs"
      - "scripts/tauri-native-updater.mjs"
      - "scripts/native-updater-acceptance.mjs"
      - "scripts/test-rust-msrv-contract.mjs"
      - "scripts/test-extension-run-history-contract.mjs"
      - "scripts/test-extension-p1-3-contract.mjs"
      - "scripts/test-installed-license-surface.mjs"
      - "scripts/test-signpath-readiness.mjs"
      - "scripts/test-signpath-free-trial-smoke.mjs"
      - "scripts/test-signpath-candidate-workflow.mjs"
      - "scripts/test-conditional-prerelease-policy.mjs"
      - "scripts/test-tauri-native-updater-contract.mjs"
      - "scripts/test-tauri-bundle-type.mjs"
      - "scripts/test-three-platform-auto-updater.mjs"
      - "scripts/test-native-updater-acceptance-transport.mjs"
      - "scripts/windows-installed-focus-acceptance.mjs"
      - "scripts/test-windows-installed-focus-acceptance.mjs"
      - "docs/design/accepted-2026-07-25-about-and-update-check-design.md"
      - "docs/README.md"
      - "docs/plans/active-2026-08-11-signpath-application-readiness-spec.md"
      - "docs/plans/active-2026-08-12-signpath-free-trial-smoke-spec.md"
      - "docs/plans/active-2026-08-13-dev38-test-signed-prerelease-spec.md"
      - "docs/plans/implemented-2026-08-13-conditional-prerelease-policy-spec.md"
      - "docs/plans/active-2026-08-15-tauri-native-updater-spec.md"
      - "docs/plans/active-2026-08-17-signpath-free-trial-two-stage-dev42-spec.md"
      - "docs/plans/implemented-2026-08-17-three-platform-automatic-updater-dev43-spec.md"
      - "docs/plans/implemented-2026-08-17-stable-0.4.0-release-spec.md"
      - "docs/project/active-document-cross-review.md"
      - "docs/project/active-development-roadmap.md"
      - "docs/release/active-0.4.0-dev.37-candidate-checklist.md"
      - "docs/release/active-0.4.0-dev.38-candidate-checklist.md"
      - "docs/release/historical-0.4.0-dev.39-candidate-checklist.md"
      - "docs/release/active-0.4.0-dev.40-candidate-checklist.md"
      - "docs/release/active-0.4.0-dev.41-native-updater-acceptance-target-checklist.md"
      - "docs/release/active-0.4.0-dev.42-two-stage-signing-checklist.md"
      - "docs/release/historical-0.4.0-dev.43-three-platform-updater-checklist.md"
      - "docs/release/historical-0.4.0-stable-release-checklist.md"
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review]
    paths:
      - "**/Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".cargo/**"
      - "crates/**/*.rs"
      - "desktop/src-tauri/**"
      - "R/rho.agent/R/aisdk_adapter.R"
      - "desktop/dist/app.js"
      - "desktop/dist/index.html"
      - "desktop/dist/styles.css"
      - "LICENSE"
      - "LICENSES.md"
      - "PRIVACY.md"
      - "SECURITY.md"
      - "CODE_SIGNING_POLICY.md"
      - "README.md"
      - "NEWS.md"
      - ".github/CODEOWNERS"
      - ".github/release-notes/**"
      - "vendor/jet/**/*.rs"
      - "runtime/ark.json"
      - "scripts/bootstrap-ark-macos.sh"
      - "scripts/bootstrap-ark-windows.ps1"
      - "scripts/build-windows-installer.ps1"
      - "scripts/tauri-bundle-type.mjs"
      - ".github/workflows/rust-compatibility.yml"
      - ".github/workflows/rust-fast.yml"
      - ".github/workflows/candidate-build-draft.yml"
      - ".github/workflows/candidate-publish.yml"
      - ".github/workflows/native-updater-acceptance-target.yml"
      - ".github/workflows/native-updater-acceptance-window.yml"
      - ".github/workflows/signpath-free-trial-smoke.yml"
      - ".github/workflows/windows-issue33-installed-acceptance.yml"
      - ".github/workflows/update-site-publish.yml"
      - "scripts/candidate-release.mjs"
      - "scripts/generate-update-site.mjs"
      - "scripts/macos-notary.mjs"
      - "scripts/tauri-native-updater.mjs"
      - "scripts/native-updater-acceptance.mjs"
      - "scripts/test-rust-msrv-contract.mjs"
      - "scripts/test-extension-run-history-contract.mjs"
      - "scripts/test-extension-p1-3-contract.mjs"
      - "scripts/test-installed-license-surface.mjs"
      - "scripts/test-signpath-readiness.mjs"
      - "scripts/test-signpath-free-trial-smoke.mjs"
      - "scripts/test-signpath-candidate-workflow.mjs"
      - "scripts/test-conditional-prerelease-policy.mjs"
      - "scripts/test-tauri-native-updater-contract.mjs"
      - "scripts/test-tauri-bundle-type.mjs"
      - "scripts/test-three-platform-auto-updater.mjs"
      - "scripts/test-native-updater-acceptance-transport.mjs"
      - "scripts/windows-installed-focus-acceptance.mjs"
      - "scripts/test-windows-installed-focus-acceptance.mjs"
      - "docs/design/accepted-2026-07-25-about-and-update-check-design.md"
      - "docs/README.md"
      - "docs/plans/active-2026-08-11-signpath-application-readiness-spec.md"
      - "docs/plans/active-2026-08-12-signpath-free-trial-smoke-spec.md"
      - "docs/plans/active-2026-08-13-dev38-test-signed-prerelease-spec.md"
      - "docs/plans/implemented-2026-08-13-conditional-prerelease-policy-spec.md"
      - "docs/plans/active-2026-08-15-tauri-native-updater-spec.md"
      - "docs/plans/active-2026-08-17-signpath-free-trial-two-stage-dev42-spec.md"
      - "docs/plans/implemented-2026-08-17-three-platform-automatic-updater-dev43-spec.md"
      - "docs/plans/implemented-2026-08-17-stable-0.4.0-release-spec.md"
      - "docs/project/active-document-cross-review.md"
      - "docs/project/active-development-roadmap.md"
      - "docs/release/active-0.4.0-dev.37-candidate-checklist.md"
      - "docs/release/active-0.4.0-dev.38-candidate-checklist.md"
      - "docs/release/historical-0.4.0-dev.39-candidate-checklist.md"
      - "docs/release/active-0.4.0-dev.40-candidate-checklist.md"
      - "docs/release/active-0.4.0-dev.41-native-updater-acceptance-target-checklist.md"
      - "docs/release/active-0.4.0-dev.42-two-stage-signing-checklist.md"
      - "docs/release/historical-0.4.0-dev.43-three-platform-updater-checklist.md"
      - "docs/release/historical-0.4.0-stable-release-checklist.md"

permissions:
  contents: read

concurrency:
  group: rust-compatibility-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust-compatibility:
    if: github.event_name == 'push' || github.event.pull_request.draft == false
    name: ${{ matrix.os }} / Rust ${{ matrix.toolchain }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-26
            toolchain: "stable"
            rustup_toolchain: stable-aarch64-apple-darwin
            host: aarch64-apple-darwin
          - os: macos-26
            toolchain: "1.88.0"
            rustup_toolchain: 1.88.0-aarch64-apple-darwin
            host: aarch64-apple-darwin
          - os: windows-latest
            toolchain: "stable"
            rustup_toolchain: stable-x86_64-pc-windows-gnu
            host: x86_64-pc-windows-gnu
          - os: windows-latest
            toolchain: "1.88.0"
            rustup_toolchain: 1.88.0-x86_64-pc-windows-gnu
            host: x86_64-pc-windows-gnu
          - os: ubuntu-22.04
            toolchain: "stable"
            rustup_toolchain: stable-x86_64-unknown-linux-gnu
            host: x86_64-unknown-linux-gnu
          - os: ubuntu-22.04
            toolchain: "1.88.0"
            rustup_toolchain: 1.88.0-x86_64-unknown-linux-gnu
            host: x86_64-unknown-linux-gnu
    env:
      RUSTUP_TOOLCHAIN: ${{ matrix.rustup_toolchain }}
      CARGO_INCREMENTAL: "0"

    steps:
      - name: Check out exact source
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up Windows Rtools
        if: runner.os == 'Windows'
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.6.1"
          rtools-version: "45"
          use-public-rspm: true

      - name: Configure Windows GNU linker
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $rtools = "C:\rtools45\x86_64-w64-mingw32.static.posix\bin"
          if (-not (Test-Path -LiteralPath $rtools)) {
            throw "Expected Rtools45 GNU directory was not found: $rtools"
          }
          Add-Content -LiteralPath $env:GITHUB_ENV -Value "RHO_RTOOLS_BIN=C:\rtools45\x86_64-w64-mingw32.static.posix\bin"
          Add-Content -LiteralPath $env:GITHUB_PATH -Value $rtools

      - name: Install Linux desktop build dependencies
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev libxdo-dev squashfs-tools pkg-config unzip zip

      - name: Install and verify Linux Rust toolchain
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          rustup set profile minimal
          rustup toolchain install "$RUSTUP_TOOLCHAIN" --no-self-update
          if [[ "${{ matrix.toolchain }}" == "stable" ]]; then rustup component add rustfmt --toolchain "$RUSTUP_TOOLCHAIN"; fi
          rustc -vV | grep -Fx "host: ${{ matrix.host }}"
          if [[ "${{ matrix.toolchain }}" == "1.88.0" ]]; then test "$(rustc -V | awk '{print $2}')" = "1.88.0"; fi

      - name: Install and verify macOS Rust toolchain
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          rustup set profile minimal
          rustup toolchain install "$RUSTUP_TOOLCHAIN" --no-self-update
          if [[ "${{ matrix.toolchain }}" == "stable" ]]; then
            rustup component add rustfmt --toolchain "$RUSTUP_TOOLCHAIN"
          fi
          active="$(rustup show active-toolchain)"
          [[ "$active" == "$RUSTUP_TOOLCHAIN "* ]]
          rust_version="$(rustc -V | awk '{print $2}')"
          if [[ "${{ matrix.toolchain }}" == "1.88.0" ]]; then
            test "$rust_version" = "1.88.0"
          fi
          rustc -vV | grep -Fx "host: ${{ matrix.host }}"
          cargo -vV

      - name: Install and verify Windows GNU Rust toolchain
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:PATH="$env:RHO_RTOOLS_BIN;$env:PATH"
          rustup set profile minimal
          rustup toolchain install $env:RUSTUP_TOOLCHAIN --no-self-update
          if ("${{ matrix.toolchain }}" -eq "stable") {
            rustup component add rustfmt --toolchain $env:RUSTUP_TOOLCHAIN
          }
          $active = (& rustup show active-toolchain).Trim()
          if (-not $active.StartsWith("$env:RUSTUP_TOOLCHAIN ")) {
            throw "Expected $env:RUSTUP_TOOLCHAIN, received $active"
          }
          $rustVersion = ((& rustc -V).Trim() -split '\s+')[1]
          if ("${{ matrix.toolchain }}" -eq "1.88.0" -and $rustVersion -ne "1.88.0") {
            throw "Expected rustc 1.88.0, received $rustVersion"
          }
          $hostLine = (& rustc -vV | Select-String '^host: ').Line
          if ($hostLine -ne "host: ${{ matrix.host }}") {
            throw "Expected host ${{ matrix.host }}, received $hostLine"
          }
          cargo -vV

      - name: Restore Cargo dependency and build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: rho-rust-v1-${{ runner.os }}-${{ env.RUSTUP_TOOLCHAIN }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            rho-rust-v1-${{ runner.os }}-${{ env.RUSTUP_TOOLCHAIN }}-

      - name: Stage pinned macOS Ark sidecar for Tauri compilation
        if: runner.os == 'macOS'
        shell: bash
        run: ./scripts/bootstrap-ark-macos.sh

      - name: Stage pinned Linux Ark sidecar for Tauri compilation
        if: runner.os == 'Linux'
        shell: bash
        run: |
          ./scripts/bootstrap-ark-linux.sh
          ./scripts/prepare-runtime-resources.sh

      - name: Enforce current stable formatting
        if: matrix.toolchain == 'stable'
        run: cargo fmt --all -- --check

      - name: Verify installed license surface
        if: matrix.toolchain == 'stable'
        run: |
          node scripts/test-installed-license-surface.mjs
          node scripts/generate-update-site.mjs --test true
          node scripts/test-signpath-readiness.mjs --self-test
          node scripts/test-signpath-readiness.mjs
          node scripts/test-signpath-free-trial-smoke.mjs --self-test
          node scripts/test-signpath-free-trial-smoke.mjs
          node scripts/candidate-release.mjs --test true
          node scripts/test-signpath-candidate-workflow.mjs --self-test
          node scripts/test-signpath-candidate-workflow.mjs
          node scripts/test-conditional-prerelease-policy.mjs
          node --check desktop/dist/app.js
          node scripts/test-macos-notary.mjs
          node scripts/test-mac4-release-contract.mjs
          node scripts/test-tauri-native-updater-contract.mjs
          node scripts/test-native-updater-acceptance-transport.mjs
          node scripts/test-three-platform-auto-updater.mjs

      - name: Verify locked workspace at selected Rust version
        run: |
          node scripts/test-rust-msrv-contract.mjs
          cargo check --workspace --all-targets --locked
          cargo test --workspace --locked --no-fail-fast
